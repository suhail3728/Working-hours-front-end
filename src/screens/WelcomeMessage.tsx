import React from "react";
import { View,Text } from "react-native";
import { WelcomeMessageProps } from '../types/types';

const WelcomeMessage = ({navigation}:WelcomeMessageProps) => {
return(

    <View>
        <Text>Hey</Text>
    </View>

);
    
};

export default WelcomeMessage;